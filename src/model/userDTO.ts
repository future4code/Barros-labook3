export interface userInputDTO {
    name: string;
    email: string;
    password: string;
}

export interface userInsertDTO {
    id: string;
    name: string;
    email: string;
    password: string;
}